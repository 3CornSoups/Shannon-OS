# Shannon environment configuration (secrets recommended here)
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_API_KEY=sk-xxxxxxxx

# Default SSH port used by host form when port is empty
SHANNON_DEFAULT_SSH_PORT=22

# Optional web service port
SHANNON_PORT=8000

# 监控采集间隔（秒），默认 60，最低 10
SHANNON_MONITOR_INTERVAL=60

# 钉钉通知（可选）
SHANNON_DINGTALK_WEBHOOK_URL=
SHANNON_DINGTALK_SECRET=

# 邮件通知（可选）
SHANNON_SMTP_HOST=
SHANNON_SMTP_PORT=465
SHANNON_SMTP_USERNAME=
SHANNON_SMTP_PASSWORD=
SHANNON_SMTP_RECIPIENTS=
