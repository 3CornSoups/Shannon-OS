# Shannon OS Agent 配置模板
#
# 注意：.env 文件是可选的！
# 部署完成后，你可以直接在 Web 前端的「设置」页面中配置：
#   - DeepSeek API Key
#   - API Base URL
#   - 模型名称
#   - 默认 SSH 端口
#
# 服务器信息（SSH 主机、端口、用户名、密码/私钥）在「服务器管理」页面配置。
#
# 仅当你想通过环境变量预设配置时，才需要创建 .env 文件。

# DeepSeek API 配置（也可在 Web 设置页面配置）
# DEEPSEEK_API_BASE=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_API_KEY=sk-your-api-key-here

# 默认 SSH 端口（Web 设置页面可单独配置每台服务器）
# SHANNON_DEFAULT_SSH_PORT=22

# Web 服务端口
# SHANNON_PORT=8000
