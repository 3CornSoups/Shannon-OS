# Shannon OS Agent — 技术思路

## 1. 总体设计理念

Shannon OS Agent 的本质是在 LLM 与 Linux 服务器之间构建一个 **AIOS（AI Operating System）语义抽象层**。传统运维路径是「人 → CLI → 服务器」，AIOS 将其替换为「人 → 自然语言 → AI Agent → 工具调用 → 服务器」，使 Agent 承担起类似操作系统内核的调度角色——接收高层意图，分解为原子操作，调度执行资源，处理异常并报告结果。系统遵循三条原则：**LLM 自主决策优先于规则引擎**（意图分类、工具选择、终止条件、委托决策全由模型判断，规则只做安全兜底）；**流式优先**（所有长耗时操作通过 SSE 实时推送）；**连接即会话**（委托期间 SSH 连接独占，保持 PTY 会话连续性）。

## 2. ReAct 循环：AIOS 的执行引擎

系统采用 ReAct（Reasoning + Acting）范式，让 Agent 在每一步经历「思考-行动-观察」闭环，而非一轮调用生成完整计划后批量执行。LLM 通过 Function Calling 选择工具，系统注册了四个工具：`execute_command`（系统级 shell 操作）、`delegate_task`（将代码任务委托给子智能体）、`ask_user`（请求用户介入）、`task_done`（判断完成并终止）。工具粒度的权衡经过实践收敛——太粗则意图模糊，太细则 token 开销失控。循环终止采用三层策略：LLM 自主判断目标达成并调用 `task_done` 作为主路径，40 轮硬限制作为安全兜底，重复命令检测防止在错误路径上反复尝试。

## 3. 智能委托：多智能体协作

单一 Agent 难以同时胜任「系统运维」和「代码工程」两类任务。用 shell 命令做代码分析远不如专用代码智能体，反之亦然。系统引入子智能体委托机制：LLM 通过 `delegate_task` 工具自主判断是否委托，运行时动态探测远程服务器上的可用子智能体（Claude Code 等）并自动注册。委托判断原则在 System Prompt 中引导（代码分析/重构/依赖追踪走委托，运维操作 Agent 自己做），不在代码中硬编码分类规则。委托期间 SSH 连接独占以保证 PTY 会话连续性，新消息到达时通过冲突机制让用户选择取消或排队。子智能体的长输出（超过 8000 字符）自动分段提交 LLM 做二次审核：每段先提取摘要，再汇总做最终的目标达成判断。

## 4. PTY 双向交互与安全风控

普通 SSH 命令执行缺少交互能力，但子智能体执行中可能遇到 `sudo` 密码、git commit 编辑器、权限确认提示等需要用户介入的场景。PTY 伪终端模式赋予子智能体完整的交互环境。权限提示的转发链路为：PTY 输出流 → 正则识别权限提示 → SSE 事件推送前端 → 用户点击同意/拒绝 → 决定写回 PTY 输入。这一链路涉及异步流解析、事件同步和超时处理，任一环节延迟或丢失都可能导致委托卡死或越权。

安全方面采用四层纵深防御：第一层硬阻断清单（裸盘写入、fork 炸弹、curl-pipe-shell 等命中后绝对不执行）；第二层 LLM 风险标注（每次 execute_command 标注 LOW/HIGH，HIGH 需用户确认）；第三层委托前后双审计（委托前 LLM 标注风险等级，委托后审核器提取已执行命令做二次审计）；第四层操作日志（所有命令写入 `operation_logs` 表，确认/拒绝记录写入 `audit_records` 表）。

## 5. SSE 事件总线

选择 SSE 而非 WebSocket，因为 Agent 场景的通信是单向的（服务端推送进度），前端仅需少量反向操作（取消、确认），SSE 更轻量且与 FastAPI 集成更自然。EventStore 实现了事件缓存与重放：前端未连接时缓存，连接后重放，过期事件（超 10 分钟）自动清理。系统定义了 10 种 SSE 事件类型，覆盖委托全生命周期：`delegate_confirm_required` → `delegate_started` → `delegate_progress` → `delegate_completed` / `delegate_cancelled` / `delegate_timeout` → `delegate_review`，以及 `delegate_install_required`、`delegate_permission_required`、`delegation_conflict`。

## 6. SSH 连接池与前端架构

SSH 连接池采用 asyncssh（异步）+ paramiko（同步）双引擎：默认 asyncssh，连续失败 2 次自动降级到 paramiko，兼容各类 SSH 实现；连接复用前通过 `echo alive` 验证存活；后台任务每 60 秒回收超过 5 分钟的空闲连接。

前端采用 Vue 3 + Pinia + Tailwind CSS。委托状态通过 Pinia store 管理状态机（idle → suggested → running → completed/cancelled/timeout/fallback），DelegationCard 组件根据状态渲染不同 UI，安装引导、冲突解决、权限确认三个弹窗互斥确保用户面对单一决策点。Terminal 组件基于 xterm.js 实现 Web 终端，支持自适应缩放和 ANSI 渲染。
