# Shannon OS Agent — 设计重点与难点

***

## 1. AIOS 语义层的抽象设计

本项目的核心挑战在于构建一套将自然语言意图转化为服务器操作指令的 AIOS（AI Operating System）语义抽象层。不同于传统的规则引擎或脚本编排，系统需要让 LLM 在 ReAct 循环中自主完成「理解意图 → 分解子任务 → 选择工具 → 执行观察 → 判断终止」的完整决策链。这要求 prompt 工程、工具定义（tool calling schema）和状态管理三者高度协同：工具粒度太粗则 AI 无法精确操作，太细则 token 开销失控；ReAct 循环缺少终止条件容易陷入无限循环，过于保守则任务半途而废。我们在实践中沉淀出了一套分层工具设计——`execute_command` 负责系统级操作、`delegate_task` 负责代码级重构、`ask_user` 和 `task_done` 构成人机回环的闭环——使 AI Agent 具备了接近操作系统内核的调度能力。

## 2. 智能委托与多智能体协作

单一 Agent 难以同时胜任「系统运维」和「代码工程」两类差异巨大的任务。我们设计了一套动态子智能体委托机制：Agent 通过 LLM 自主判断任务类型，当识别到更适合某类智能体处理的任务时，系统能智能调用远程服务器上的子智能体进行操作。比如：当识别到复杂的代码重构、多文件编辑等编程场景时，自动探测远程服务器上可用的 Claude Code CLI 并将其作为子智能体调用。这里的难点在于：

- **委托边界的模糊性**：什么时候该调用子智能体并非总是泾渭分明，需要在 prompt 中建立清晰的判断原则。
- **连接独占与冲突处理**：委托期间 SSH 连接不归还池以保证流式输出，但新请求到达时需要在「取消-排队」之间做出合理选择。
- **子智能体的输出审核**：子智能体的长输出分段提交 LLM 做二次审核，既要保证安全性又不能让审核本身成为瓶颈。

## 3. PTY 双向交互与安全风控的平衡

SSH 的 PTY 伪终端模式让子智能体获得了交互式执行能力（如 git commit 需要编辑器、权限提升需要 sudo 密码），但也引入了新的安全面。系统需要实时解析 PTY 输出流中的权限提示（如 Claude Code 的 `Do you want to proceed?`），将其转为 SSE 事件推送给用户确认，再将用户决定转回 PTY 输入。这一链路涉及异步流解析、正则模式匹配、事件同步和超时处理，任一环节的延迟或丢失都可能导致委托卡死或越过用户授权。配合前端的权限确认条 UI 和后端的 HIGH\_RISK\_KEYWORDS 审计层，我们在「自动化效率」和「人工可控」之间找到了一个实用平衡点。
